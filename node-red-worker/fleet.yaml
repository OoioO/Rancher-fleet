defaultNamespace: node-red

helm:
  chart: node-red-worker
  repo: https://alberthahn.github.io/Krones-Marketplace
  releaseName: node-red-fleet-worker
  version: 1.0.1
  values:
    clusterName: global.fleet.clusterLabels.management.cattle.io/cluster-display-name
    ingress:
      enabled: true
      host: node-red-fleet-worker.{{ .Values.clusterName }}.plgrnd.be